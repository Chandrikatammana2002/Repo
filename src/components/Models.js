import React from 'react'
import models from "../assests/models.png"

function Models() {
  return (
    <div className='models'>
      <img src={models} alt="models"/>
    </div>
  )
}

export default Models
